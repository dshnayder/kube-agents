apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kage-pvc
  namespace: agent-system
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 40Gi
  storageClassName: standard-rwo
  volumeMode: Filesystem
